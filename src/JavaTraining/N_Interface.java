/**
 * 
 */
package JavaTraining;

/**
 * @author Raj Kamal
 *
 *this is Interface
 *only method Declaration
 *no Method body, only prototype
 *in Interface, we can declare a variable, which is static by default
 *variable value will not change
 *no static methods is interface
 *no main method in interface
 *we can create object in interface
 *interface is abstract in nature
 *
 *
 *Abstract : we can't instantiate, which means no object can be created
 *
 */
public interface N_Interface {
	
	int minbal = 100;
	public void credit();
	public void debit();
	public void transfer();
	

}
